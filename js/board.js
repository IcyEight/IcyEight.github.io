$(window).load(function() {
	// load board
});

// checks to see who's turn it is and prompts accordingly
function isTurn() {
	/*
	if is turn is true {
		display message saying your turn
	}
	else {
		display message saying opponent's turn
	}
	*/
}

// called after move has been made
function validateMove() {
	/*
	if validMove {
		checkForWinner();
		if winner() {
			if this player is winner {
				window.location.href = "win.html";
			}
			else if this player is loser {
				window.location.href = "lose.html";
			}
			else {
				window.location.href = "draw.html";
			}
		}
		else {
			refreshBoard();
		}
	}
	else {
		invalidMove();
	}
	*/
}

function refreshBoard() {
	// ajax call to get state of board
}